# Skill 工程化原则

当创建或维护 Pi Skill 时，遵循以下设计规范，确保 skill 是可迁移、可复用的自包含单元。

> **一个可迁移复用的 skill 应该是自包含的单元，而不是散落的指引+外部文件。**

## 核心要点

### 自包含封装
脚本、参考模板、配置文件必须与 `SKILL.md` 打包在一起，确保可迁移复用。

### 一个 skill = 一个完整工具箱
用户安装后无需额外查找资源，开箱即用。

### 避免外部依赖
除非是标准库或通用包（npm/pyPI），否则所有必要资源都应嵌入 skill 目录。
